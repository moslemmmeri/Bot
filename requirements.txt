# ============================================================
# requirements.txt
# وابستگی‌های پروژه ربات
# ============================================================

# ============================================================
# وابستگی‌های اصلی
# ============================================================

# HTTP Client برای ارتباط با بله API
aiohttp>=3.9.0

# مدیریت متغیرهای محیطی
python-dotenv>=1.0.0

# تسک‌های زمان‌بندی‌شده
APScheduler>=3.10.0

# Redis Client (برای کش و Rate Limiting)
redis>=5.0.0

# تولید فایل‌های Excel
openpyxl>=3.1.0

# تولید نمودارهای گرافیکی
matplotlib>=3.7.0

# ============================================================
# درایورهای دیتابیس (اختیاری - بسته به نیاز)
# ============================================================

# PostgreSQL (اختیاری)
asyncpg>=0.29.0

# MySQL (اختیاری)
aiomysql>=0.2.0

# ============================================================
# تست و توسعه (اختیاری - برای محیط توسعه)
# ============================================================

# تست‌های واحد (اختیاری)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# ============================================================
# کیفیت کد (اختیاری - برای محیط توسعه)
# ============================================================

# تحلیل کیفیت کد
pylint>=3.0.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0

# ============================================================
# نصب وابستگی‌ها
# ============================================================
#
# برای نصب تمام وابستگی‌ها:
# pip install -r requirements.txt
#
# برای نصب فقط وابستگی‌های اصلی (بدون درایورهای دیتابیس و تست):
# pip install aiohttp python-dotenv APScheduler redis openpyxl matplotlib
#
# برای نصب در محیط تولید (بدون وابستگی‌های توسعه):
# pip install -r requirements.txt --no-deps
# یا با ایجاد یک فایل requirements-prod.txt جداگانه
#
# ============================================================
# وابستگی‌های پیشنهادی برای محیط تولید
# ============================================================
#
# برای محیط تولید، وابستگی‌های زیر را نیز نصب کنید:
# - gunicorn (برای اجرای وب‌سرویس در صورت نیاز)
# - uvicorn (برای اجرای FastAPI در صورت نیاز)
#
# ============================================================
# نکته: برای اجرا در محیط‌های مختلف
# ============================================================
#
# محیط توسعه:
# pip install -r requirements.txt
#
# محیط تولید (حداقل وابستگی):
# pip install aiohttp python-dotenv APScheduler redis openpyxl matplotlib
#
# محیط تست:
# pip install -r requirements.txt && pip install pytest pytest-asyncio pytest-cov
#
# ============================================================